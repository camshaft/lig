
module.exports = function(fn) {
  // Call fn for each message in current buffer
  // subscribe to any new messages

  // return a fn to unsubscribe
  return unsubscribe;

  unsubscribe.setFilters = function() {

  };

  function unsubscribe() {

  };
};
